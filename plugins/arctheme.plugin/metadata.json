{
	"label": "Arc Theme",
	"description": "Tema com design flat.",
	"license": "GPLv3",
	"category": "Temas",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "dpkg-query -W -f='${Status}' arc-theme 2>/dev/null | grep -c 'ok installed'" }
	}
}
